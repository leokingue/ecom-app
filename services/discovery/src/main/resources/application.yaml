spring:
  config:
    # Provide the path at the Discovery Service to fetch configurations ( config-server/resources/configurations/discovery-service.yaml )
    import: optional:configserver:http://localhost:8888
  application:
    name: discovery-service
